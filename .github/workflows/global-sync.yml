name: Global Synchronization

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  sync-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Generate checksums for binaries
        run: |
          echo "## Binary Asset Checksums" > checksums.txt
          echo "" >> checksums.txt
          find . -type f -name "*.bin" | while read file; do
            echo "### $file" >> checksums.txt
            sha256sum "$file" >> checksums.txt
            echo "" >> checksums.txt
          done
          cat checksums.txt
          
      - name: Create Release Archive
        run: |
          mkdir -p release-assets
          cp -r *.bin *.html *.md LICENSE release-assets/ 2>/dev/null || true
          cp checksums.txt release-assets/
          tar -czf release-$(date +%Y%m%d-%H%M%S).tar.gz release-assets/
          
      - name: Upload Release Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-package
          path: |
            release-*.tar.gz
            checksums.txt
          retention-days: 90
          
      - name: Sync Status Report
        run: |
          echo "✅ Global synchronization completed"
          echo "📦 Release package created with all binaries and assets"
          echo "🔐 Checksums generated for integrity verification"
          echo "📤 Artifacts uploaded and ready for distribution"

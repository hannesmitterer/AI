{
  "name": "aufhor-token",
  "version": "1.0.0",
  "description": "AUFHOR (AH) – ERC-20 token representing sovereign time, deployed on Optimism L2.",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network optimism-sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network optimism",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
